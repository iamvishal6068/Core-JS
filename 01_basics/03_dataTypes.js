"use Strict"; // it treats all js code as a new version.

// alert( 3 + 3 ) // we are using node js not a browser bcz if we write this on console in browser it gives us a output of 6, but not here.
// (it gives us not defined)
let name ="Vishal" // string 
let mobNum= 9988443551; // int 
let isLoggedIn = true; //boolean
let bigInt = 122789654; //bigInt
let value; //it gives us a undefined value bcz we didnt assign any value to the variable.
let num = null; //it gives us a object as a output.


/*
These are primitive data types of js
*/
//number=> 2 to the power 53
//null=> it is a standalone value
//undefined => 
// symbol => unique
// bigint => 
// boolean
// string => " "

// object => {}
    let obj ={}
    console.log(typeof null );

    let password = "0123456789"
    console.log(typeof password)